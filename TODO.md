# Task: Make menu work with category-based commands

## Steps
- [x] Analyze codebase and create plan
- [x] Confirm plan with user

## Step 1: Modify `handler.js`
- [x] Pass `commandName` in execute extra context

## Step 2: Modify `commands/general/menu.js`
- [x] Add all category names as aliases
- [x] `/menu` or `/help` → Show category overview
- [x] `/general`, `/admin`, `/ai`, `/fun`, `/media`, `/owner`, `/textmaker`, `/utility` → Show category-specific commands
- [x] `/menu general`, `/menu admin` etc. → Also work
- [x] Keep all existing features (image, newsletter, mentions, footer)

## Step 3: Verify
- [x] Implementation complete

